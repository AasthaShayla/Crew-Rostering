server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve static assets
  location / {
    try_files $uri /index.html;
  }

  # Optional: if you later want to proxy API via same origin,
  # uncomment and ensure compose service is named 'backend'.
  # location /api/ {
  #   proxy_pass http://backend:5050/api/;
  #   proxy_http_version 1.1;
  #   proxy_set_header Upgrade $http_upgrade;
  #   proxy_set_header Connection "upgrade";
  #   proxy_set_header Host $host;
  # }

  # Optional: Socket.IO proxy (only if you choose to go same-origin)
  # location /socket.io/ {
  #   proxy_pass http://backend:5050/socket.io/;
  #   proxy_http_version 1.1;
  #   proxy_set_header Upgrade $http_upgrade;
  #   proxy_set_header Connection "upgrade";
  #   proxy_set_header Host $host;
  # }
}

